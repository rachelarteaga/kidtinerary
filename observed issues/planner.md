# Planner

The weekly planner view.

## Issues

- ~~**Cost paid not centered on status button** — on the camp card in the planner, the "cost paid" value isn't centered within the status button.~~ Fixed 2026-04-23 (commit on `account-menu-updates`): added matching `py-0.5 border border-transparent leading-none` to the price span in `planner-cell.tsx` and `shared-planner-view.tsx` so it boxes to the same height as the status pill.
- ~~**Camp name shows text cursor instead of pointer on hover** — clickable camp names in the planner should use `cursor-pointer`. (See cross-cutting note for other spots where this is missing.)~~ Fixed 2026-04-23 (commit on `account-menu-updates`): added `cursor-pointer` to the legend-row buttons in `planner-cell.tsx` and `shared-planner-view.tsx`, plus the considering-chip buttons in `considering-chips.tsx`. Cross-cutting items (camp detail drawer name/org + modal backdrops) still open.
- ~~**Drag-to-block status selector floats away from drop location**~~ — Fixed 2026-04-23 (branch `fix/observed-issues-polish`): rewrote [status-picker-popover.tsx](src/components/planner/status-picker-popover.tsx) as an on-cell overlay (bg-white/75 with backdrop-blur), vertically centered on the cell midpoint via `translateY(-50%)` so the card expands evenly above/below as content grows. Header reads "SELECT STATUS" + camp name; top-right close X; Escape and click-outside still cancel.
- ~~**Status selector options should be color-coded**~~ — Fixed 2026-04-23 (same branch): options now render as full-word tinted pills using the status tokens (`bg-status-registered`/`-waitlisted`/`-considering`). Pills stack vertically via container query when the cell is < 260px. As part of this: registered status color swapped gold → jade `#5ac195` in [globals.css](src/app/globals.css) + [status-dropdown.tsx](src/components/planner/status-dropdown.tsx) (hero gold unchanged for nav/brand).
- **"Last edited" marker logic needs verification on "my planners"** — timestamp may not be updating correctly (e.g. on status changes, considering-chip edits, drag-drop, cost edits). Audit what counts as an edit and confirm the field updates on all of them; also check timezone/relative-time formatting.
